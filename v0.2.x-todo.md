# v0.2.x todo

Small fixes deferred from earlier sessions. Newest at the top.

## Done

- ~~**Badge format ≥ 10 minutes.**~~ Fixed in session 5. Shows `M` (minutes only) at 10+, `M:SS` under 10.
- ~~**Subdomain override mismatch.**~~ Fixed in session 5. `hasActiveOverride` now applies the same parent-covers-child subdomain logic as `isHostBlocked`.
- ~~**Show active overrides in popup.**~~ Fixed in session 5. Popup now displays active overrides with hosts + countdowns, sorted by soonest-expiring, auto-clears when none active.

## Open

- **Service worker tick reliability.** `setInterval` dies when the service worker sleeps. Functional today because the popup re-pings, but should migrate to `chrome.alarms` for honest robustness.