# v0.2.x todo

Small fixes deferred from session 4. Not blocking; pick up when convenient.

- **Badge format ≥ 10 minutes.** Currently shows `10:00` clipped at 4 chars. Switch to minutes-only above 10 (`15`, `14`, ... `10`, `9:59`).

- **Subdomain override mismatch.** `isHostBlocked` matches subdomains (`www.claude.ai` matches `claude.ai`). `hasActiveOverride` does exact host match. So overriding `claude.ai` doesn't cover `www.claude.ai`. Either normalize hosts before storing/checking, or apply the same subdomain logic to overrides.

- **Service worker tick reliability.** `setInterval` dies when the service worker sleeps. Functional today because the popup re-pings, but should migrate to `chrome.alarms` for honest robustness.