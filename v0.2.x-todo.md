# v0.2.x todo

Small fixes deferred from earlier sessions. Newest at the top.

## Completed (session 7)

- ~~Time-picker in popup.~~ Done. Two number inputs (work/open minutes) at the top of the popup, validated 1-60 integers, disabled while a session is running. Values seeded once on popup open, then owned by the user until Start. Last-used values persist via `state.workMinutes` / `state.openMinutes` and survive Reset.
- ~~Override duration label off-by-one ("1 minute" instead of "2").~~ Done. `Math.floor` on a value reduced by message round-trip latency rounded down to 1. Switched to `Math.ceil`.
- ~~Override timer drained on wall clock during pause.~~ Done. Pause now freezes each active override into `{host, pausedRemainingMs}`. Resume converts back to `{host, expiresAt: now + pausedRemainingMs}`. Badge shows "PAUS" while paused regardless of overrides; popup shows host with "PAUSED" label.
- ~~Reset wiped duration preferences back to defaults.~~ Done. Reset now preserves `workMinutes` and `openMinutes` alongside the blocklist.
- ~~`previouslyActive` variable named misleadingly.~~ Done as part of fix 2. Now filters on `o.expiresAt != null`, which correctly captures "could plausibly expire."

## Completed (session 6)

- ~~Service worker tick reliability.~~ Done. Migrated from `setInterval` to `chrome.alarms` with 30-second period.
- ~~Starting a session doesn't block already-open tabs.~~ Done. `startSession` and `resume` scan open tabs and force-redirect blocked hosts.
- ~~Pausing/resetting doesn't clear existing block pages.~~ Done.

## Completed (session 5)

- ~~Badge format ≥ 10 minutes.~~ Done.
- ~~Subdomain override mismatch.~~ Done.
- ~~Popup didn't show active overrides.~~ Done.

## Outstanding

- **Override symmetry.** Overriding a root (`claude.ai`) covers subdomains. The reverse doesn't.

- **Pause behavior under review.** Currently `pause` clears block pages. May want to preserve them with a frozen countdown later if pause stays as a feature. Pause may also be removed entirely in a cleanup pass.

- **30-second tick granularity may feel laggy.** Real trade-off of the alarms migration. Hybrid (alarms + popup-driven faster updates) is an option if needed.

- **Badge "PAUS" is truncated.** 4-char badge limit. Could use a pause emoji or single-character symbol if "PAUS" feels ugly in real use.